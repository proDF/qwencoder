<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eventrecorder.repository.EventMapper">

    <!-- Result Map -->
    <resultMap id="BaseResultMap" type="com.eventrecorder.entity.Event">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="category" property="category"/>
        <result column="event_date" property="eventDate"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- Base Column List -->
    <sql id="Base_Column_List">
        id, title, content, category, event_date, created_at, updated_at
    </sql>

</mapper>
